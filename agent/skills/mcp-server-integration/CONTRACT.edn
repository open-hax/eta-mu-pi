(skill-contract
  (name "mcp-server-integration")
  (v "ημ.skill/mcp-server-integration@0.1.0")

  (intent "Skill: MCP Server Integration")

  (activation
    (priority 50)
    (explicit ["skill:mcp-server-integration"])
    (triggers ["mcp server integration"]))

  (governance
    (touch-layer :mutable)
    (non-override [:mission :directives :safety :license :output-shape])
    (requires-user-approval false))
)
